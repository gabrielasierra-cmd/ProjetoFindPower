<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FinPowerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoFindPower">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.ProjetoFindPower"
            android:exported="true">
        </activity>

        <activity android:name=".PartilhasActivity" android:exported="false" />
        <activity android:name=".RelatoriosActivity" android:exported="false" />
        <activity android:name=".CategoriasActivity" android:exported="false" />
        <activity android:name=".DigitalizarActivity" android:exported="false" />
        <activity android:name=".NovaMovimentacaoActivity" android:exported="false" />
        <activity android:name=".HistoricoMensalActivity" android:exported="false" />
        <activity android:name=".HistoricoAnualActivity" android:exported="false" />
        <activity android:name=".MentoriaActivity" android:exported="false" />
    </application>

</manifest>
