<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFDF6"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    tools:context=".view.MainActivity">

    <!-- Título -->
    <TextView
        android:id="@+id/titulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="FinPower"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="#004D40"
        android:layout_marginTop="32dp"/>

    <!-- Logo -->
    <ImageView
        android:id="@+id/logo"
        android:layout_width="160dp"
        android:layout_height="160dp"
        android:src="@drawable/logo"
        android:contentDescription="Logo FinPower"
        android:layout_marginTop="16dp"/>

    <!-- Frase de impacto -->
    <TextView
        android:id="@+id/frase"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Teu Dinheiro, Teu Poder"
        android:textSize="16sp"
        android:textColor="#004D40"
        android:layout_marginTop="16dp"/>

    <!-- Botão Digitalizar -->
    <Button
        android:id="@+id/btnDigitalizar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📷 Digitalizar Despesa"
        android:backgroundTint="#E0F2F1"
        android:textColor="#004D40"
        android:layout_marginTop="32dp"/>

    <!-- Botão Categorias -->
    <Button
        android:id="@+id/btnCategorias"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🗂️ Categorias"
        android:backgroundTint="#E0F2F1"
        android:textColor="#004D40"
        android:layout_marginTop="16dp"/>

    <!-- Área do Compose -->
    <androidx.compose.ui.platform.ComposeView
        android:id="@+id/composeContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp" />

    <!-- Botão Relatórios -->
    <Button
        android:id="@+id/btnRelatorios"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📊 Relatórios"
        android:backgroundTint="#E0F2F1"
        android:textColor="#004D40"
        android:layout_marginTop="16dp" />

    <!-- Botão Partilhar -->
    <Button
        android:id="@+id/btnPartilhar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🤝 Partilhar"
        android:backgroundTint="#E0F2F1"
        android:textColor="#004D40"
        android:layout_marginTop="16dp"/>

</LinearLayout>
